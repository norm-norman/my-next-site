// mui imports
import { styled } from '@mui/material/styles';
import ButtonBase from '@mui/material/ButtonBase';

export const SocialMediaIconButton = styled(ButtonBase)(({ theme }) => ({
  padding: '10px',
  color: 'white',
}));
